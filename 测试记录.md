# 测试记录

## 2026-05-26

### 测试 1：仪表盘页 — 设置按钮

| 字段 | 内容 |
|------|------|
| **测试功能** | 仪表盘页的"设置"按钮 |
| **前置条件** | 所有服务正常运行，访问 http://localhost:3000 |
| **测试步骤** | 点击仪表盘页中的"设置"按钮 |
| **预期结果** | 跳转到设置页面 |
| **实际表现** | 正常跳转至设置页，页面加载正常 |
| **结论** | ✅ 通过 |

### 测试 2：仪表盘页 — 新建对象类型按钮

| 字段 | 内容 |
|------|------|
| **测试功能** | 仪表盘页的"新建对象类型"按钮 |
| **前置条件** | 所有服务正常运行 |
| **测试步骤** | 点击"新建对象类型"按钮 |
| **预期结果** | 跳转到对象类型管理页面 |
| **实际表现** | 正常跳转至对象类型页，页面加载正常 |
| **结论** | ✅ 通过 |

### 测试 3：智能对话 — 实例查询问答

| 字段 | 内容 |
|------|------|
| **测试功能** | 智能对话页面的实例查询功能 |
| **前置条件** | MCP Server / MCP Client 已启动，DeepSeek API Key 已配置 |
| **测试步骤** | 进入智能对话页面，输入问题"电芯有哪些实例" |
| **预期结果** | 返回电芯（battery_cell）的实例列表及相关数据 |
| **实际表现** | 修复前：返回"系统目前存在技术问题，暂时无法返回实例数据"，仅返回概念定义和关联关系，缺少实例列表 |
| **修复内容** | MCP Server 的 SSE 连接超时时间过短（约 10s），DeepSeek API 响应时间长导致连接断开。在 `application.yml` 中添加 `spring.mvc.async.request-timeout: 120000` 修复。 |
| **修复后结果** | 正确返回 1 个实例（电芯行业通用实例）及其完整的原材料组成结构、成本占比、上下游关联关系 |
| **结论** | ✅ 通过（已修复） |

### 测试 4：Neo4j 图谱同步

| 字段 | 内容 |
|------|------|
| **测试功能** | 设置页 Neo4j 图谱全量同步功能 |
| **前置条件** | Neo4j 已启动（`brew services start neo4j`），密码已配置为 `password` |
| **测试步骤** | 进入设置页 → Neo4j 图谱同步卡片 → 点击全量同步 |
| **预期结果** | 对象类型、链接类型、实例、链接实例全部成功同步至 Neo4j |
| **实际表现** | 对象类型 56 成功，链接类型 12 成功，实例 42 成功，链接实例 22 成功 / 40 跳过（实例缺失） |
| **原因分析** | `link_instance_data` 表中 40 条记录的 `source_instance_id` / `target_instance_id` 引用了不存在的实例（悬空引用），同步逻辑要求源和目标实例均在 Neo4j 中才创建关系。属历史数据遗留问题。 |
| **结论** | ⏸ 搁置（数据质量问题，非功能缺陷） |

### 测试 5：对象类型页 — 审核变更按钮

| 字段 | 内容 |
|------|------|
| **测试功能** | 对象类型页的"审核变更"按钮 |
| **前置条件** | 存在待审核的变更 |
| **测试步骤** | ① 执行审核变更操作 → ② 查看页面上是否立即显示审核通过后的结果 |
| **预期结果** | 审核后页面自动刷新，立即显示审核后的结果 |
| **实际表现** | 需要手动刷新页面才能显示审核结果 |
| **原因分析** | 待排查：推测前端审核操作完成后未触发数据重新拉取/状态更新 |
| **结论** | 🔴 待排查 |

### 测试 6：对象浏览器页 — 下拉筛选（正极材料）

| 字段 | 内容 |
|------|------|
| **测试功能** | 对象浏览器页的下拉筛选框，筛选"正极材料" |
| **前置条件** | BFF 服务正常运行 |
| **测试步骤** | 进入对象浏览器页 → 下拉筛选框 → 选择"正极材料" |
| **预期结果** | 正常显示正极材料的所有实例数据 |
| **实际表现** | 修复前：报错 `获取实例数据失败: You have an error in your SQL syntax; near 'FROM cathode_material LIMIT 100'` |
| **修复内容** | `cathode_material` 为抽象父类，`backing_dataset` 被错误设为 `'cathode_material'`，已清空，与 `raw_materials` 等其他父类保持一致 |
| **修复后结果** | 选择"正极材料"不再报错，返回空数据提示（父类无实例） |
| **结论** | ✅ 通过（已修复） |

### 测试 10：对象浏览器页 — 关系图谱（无 comcode 的公司实例）

| 字段 | 内容 |
|------|------|
| **测试功能** | 对象浏览器中公司类型的关系图谱功能 |
| **前置条件** | 存在无 `comcode` 的公司实例（如美国埃克森美孚石油公司） |
| **测试步骤** | ① 点击无 `comcode` 的公司 → ② 切到有数据的公司 → ③ 切回无 `comcode` 的公司 |
| **预期结果** | 始终显示当前选中实例的图谱，无关联关系时显示单节点卡片 |
| **实际表现** | 修复前：无 `comcode` 的公司显示"暂无关系数据"，切回后显示上一家公司的残留数据 |
| **修复内容** | `fetchGraphData` 中 `instanceId` 为空时，构造含实例数据的单节点图谱并返回，不再直接 return 导致残留数据 |
| **修复后结果** | 每个公司都显示自己的图谱节点卡片，无关联关系时仅显示当前节点 |
| **结论** | ✅ 通过（已修复） |

### 测试 7：本体图谱页 — 只读态/编辑态切换

| 字段 | 内容 |
|------|------|
| **测试功能** | 本体图谱页的只读态/编辑态切换和编辑功能 |
| **测试步骤** | 进入本体图谱页 → 切换编辑态 → 进行编辑 → 切回只读态 |
| **预期结果** | 两种状态切换正常，编辑功能可用 |
| **实际表现** | 只读态/编辑态切换正常，编辑功能正常 |
| **结论** | ✅ 通过 |

### 测试 8：对象类型页 — 新建对象类型

| 字段 | 内容 |
|------|------|
| **测试功能** | 对象类型页的新建对象类型功能 |
| **测试步骤** | 进入对象类型页 → 新建对象类型 → 填写信息 → 保存 |
| **预期结果** | 成功创建新的对象类型 |
| **实际表现** | 可以正常新建对象类型 |
| **结论** | ✅ 通过 |

### 测试 9：对象类型页 — 操作项

| 字段 | 内容 |
|------|------|
| **测试功能** | 对象类型页各操作项功能 |
| **测试步骤** | 逐一测试每个操作项 |
| **预期结果** | 每个操作项均可正常操作 |
| **实际表现** | 每个操作项均可正常操作 |
| **结论** | ✅ 通过 |

---

## 2026-05-27（续）

### 测试 8：动作类型 — 执行"创建电池实例"显示参数

| 字段 | 内容 |
|------|------|
| **测试功能** | 动作类型页执行"创建电池实例"，确认弹窗显示输入参数 |
| **前置条件** | 后端服务正常运行 |
| **测试步骤** | 点击"创建电池实例"动作的执行按钮 |
| **预期结果** | 弹窗显示输入参数字段（name、unique_id）供填写 |
| **实际表现** | 修复前：弹窗显示"此动作类型无需输入参数"。原因：数据库中有两个同名"创建电池实例"动作类型，模板自动创建的 `act_power_battery_create`（正常），和手动创建的 `act_1777014337672`（引用了不存在的本体规则 `rule_event_entity_create`）。点击到后者时参数推导链路全部走空。 |
| **修复内容** | 删除重复的动作类型 `act_1777014337672` 及其关联的 action_rule 记录。 |
| **修复后结果** | 点击"创建电池实例"后弹窗正常显示 `name`（名称）和 `unique_id`（唯一识别id）两个输入字段 |
| **结论** | ✅ 通过（已修复） |

### 测试 9：本体规则页 — 孤儿规则清理

| 字段 | 内容 |
|------|------|
| **测试功能** | 本体规则页的关联本体显示完整性 |
| **前置条件** | 后端已应用孤儿规则清理代码，重启生效 |
| **测试步骤** | ① 进入本体规则页 → ② 观察所有链接类规则的"关联本体"列是否已关联 → ③ 点击"同步规则"按钮确认正常运行 |
| **预期结果** | 所有链接类规则均有对应的关联链接类型，无"未关联"情况 |
| **实际表现** | 修复前：部分链接规则（如 `createLtBatteryCaseIsRawMaterialOfPowerBattery`）显示"未关联本体"。原因是链接类型被删除后对应的 `ontology_rules` 未清理。共发现 13 个已不存在的链接类型/对象类型引用，51 条孤儿规则。 |
| **修复内容** | 在 `RuleTemplateService.syncAllRules()` 末尾新增 `cleanupOrphanedRules()`：遍历所有 `ontology_rules`，提取 `interface_url` 中的实体 ID，若在 `link_types`/`object_types` 中已不存在则删除规则及其关联参数、动作类型。作为 `syncAllRules` 的常规步骤，每次后端重启或手动同步时自动执行。 |
| **修复后结果** | 链接类规则从 129 条降至 78 条（39 个现有链接类型 × create + delete），全部正确关联，无孤儿残留 |
| **结论** | ✅ 通过（已修复） |

### 测试 10：智能对话 — 实例查询问答（SSE 连接稳定性）

| 字段 | 内容 |
|------|------|
| **测试功能** | 智能对话页面的实例查询功能，验证 SSE 连接在代理环境下的稳定性 |
| **前置条件** | MCP Server / MCP Client 已启动，`no_proxy=localhost,127.0.0.1` 已设置 |
| **测试步骤** | 进入智能对话页面，输入问题"碳酸锂价格上涨20%对下游材料价格有何影响？" |
| **预期结果** | 正确返回碳酸锂及其下游材料的关联关系和分析结果 |
| **实际表现** | 修复前：返回 `MCP server is unavailable. Please try again later.`。排查发现系统代理 `http_proxy=http://127.0.0.1:59527` 拦截了 localhost 的 SSE 长连接，加上 MCP Client 重连时有并发冲突（两次 `connect()` 同时执行），导致重连失败。 |
| **修复内容** | ① `start.sh` 中 MCP Client 启动前加 `no_proxy=localhost,127.0.0.1` 绕过代理 ② `mcp-client/main.py` 新增 `_reconnecting` 标志位，`connect()` 入口检查是否有重连进行中，有则等待；新增 `_cleanup()` 方法，重连前先清理旧 session |
| **修复后结果** | MCP Client 正常连接，SSE 长连接稳定，可正常调用工具返回图谱数据 |
| **结论** | ✅ 通过（已修复） |

---

## 2026-05-27

### 测试 1：链接类型 — 新建

| 字段 | 内容 |
|------|------|
| **测试功能** | 链接类型的新建功能 |
| **前置条件** | 所有服务正常运行 |
| **测试步骤** | 点击"新建链接类型"按钮 → 填写信息 → 保存 |
| **预期结果** | 成功创建新的链接类型 |
| **实际表现** | 可以正常新建链接类型 |
| **结论** | ✅ 通过 |

### 测试 2：链接类型 — 修改（类别设为"不设置"）

| 字段 | 内容 |
|------|------|
| **测试功能** | 链接类型编辑时，类别可设为"不设置" |
| **前置条件** | 存在已有类别的链接类型 |
| **测试步骤** | 编辑链接类型 → 类别选择"不设置" → 保存 |
| **预期结果** | 类别成功清空 |
| **实际表现** | 修复前：类别无法清空，保存后仍为原值 |
| **修复内容** | `categoryToSend || undefined` 中空字符串被转为 `undefined` 导致 JSON.stringify 丢弃该字段，后端无法收到。改为直接发送 `categoryToSend`。 |
| **修复后结果** | 类别可正常设为"不设置" |
| **结论** | ✅ 通过（已修复） |

### 测试 3：链接类型 — 删除

| 字段 | 内容 |
|------|------|
| **测试功能** | 链接类型的删除功能 |
| **前置条件** | 存在可删除的链接类型 |
| **测试步骤** | 点击删除 → 确认 |
| **预期结果** | 链接类型被删除 |
| **实际表现** | 可正常删除 |
| **结论** | ✅ 通过 |

### 测试 4：动作类型 — 新建

| 字段 | 内容 |
|------|------|
| **测试功能** | 动作类型的新建功能 |
| **前置条件** | 所有服务正常运行 |
| **测试步骤** | 点击"新建动作类型"按钮 → 填写信息 → 保存 |
| **预期结果** | 成功创建新的动作类型 |
| **实际表现** | 可以正常新建动作类型 |
| **结论** | ✅ 通过 |

### 测试 5：动作类型 — 修改

| 字段 | 内容 |
|------|------|
| **测试功能** | 动作类型的修改功能 |
| **前置条件** | 存在可编辑的动作类型 |
| **测试步骤** | 编辑动作类型 → 修改信息 → 保存 |
| **预期结果** | 动作类型信息更新成功 |
| **实际表现** | 可以正常修改 |
| **结论** | ✅ 通过 |

### 测试 6：动作类型 — 删除

| 字段 | 内容 |
|------|------|
| **测试功能** | 动作类型的删除功能 |
| **前置条件** | 存在可删除的动作类型 |
| **测试步骤** | 点击删除 → 确认 |
| **预期结果** | 动作类型被删除 |
| **实际表现** | 可以正常删除 |
| **结论** | ✅ 通过 |

### 测试 7：动作类型 — 执行

| 字段 | 内容 |
|------|------|
| **测试功能** | 动作类型的执行功能 |
| **前置条件** | 存在已配置的动作类型 |
| **测试步骤** | 点击执行按钮 |
| **预期结果** | 动作成功执行并返回结果 |
| **实际表现** | 修复前：报错 `执行失败: Internal Server Error` |
| **修复内容** | 数据库中缺少 `action_executions` 表，`executionMapper.insert(execution)` 抛异常导致 500。已从 SQL dump 中提取建表语句创建。 |
| **修复后结果** | 执行成功，返回 `status: "completed"` 及 `executionId` |
| **结论** | ✅ 通过（已修复） |

### 测试 11：动作类型 - 创建实例

| 字段 | 内容 |
|------|------|
| **测试功能** | 在动作类型页执行"创建电芯实例"，验证实例数据入库 |
| **前置条件** | 后端服务正常运行，`act_battery_cell_create` 动作类型已存在 |
| **测试步骤** | 点击"创建电芯实例"动作 → 填写 `unique_id` 和 `name` → 执行 |
| **预期结果** | 实例数据写入 `battery_cell` 表，页面返回成功 |
| **实际表现** | 修复前：页面返回 `status: "completed"`，但 `battery_cell` 表中无数据。后端日志报 `BindingException: Parameter 'projectId' not found` |
| **修复内容** | `PropertyMapper` 存在两个重载的 `selectByObjectTypeId` 方法（两参数和一参数），MyBatis 按方法名注册，只注册了第一个，导致调用单参数版本时实际走到两参数版本，因缺少 `projectId` 抛异常。已移除单参数版本，统一使用带 `projectId` 的版本，所有 12 个调用处补充 `projectId` 参数（从已加载的 `ObjectType.getProjectId()` 获取）。 |
| **修复后结果** | 直接 API 调用和动作类型执行均返回成功，数据正确写入 `battery_cell` 表 |
| **结论** | ✅ 通过（已修复） |

---

## 待办项

| # | 描述 | 优先级 |
|---|------|--------|
| 1 | **关系图谱优化**：隐藏"组成项"（`*_composition_item`）中间节点，将多对多关系简化为源实例到目标实例的直连，减少冗余信息 | 中 |
| 2 | **审核变更按钮**：执行审核后页面应自动刷新审核结果，当前需手动刷新 | 低 |
| 3 | **关系图谱颜色优化**：DFS 遍历导致直接关联节点也显示为深一层颜色（如三元→正极材料组成项001 显示为黄色而非绿色）。建议改为按对象类型着色或 BFS 遍历 | 低 |
| 4 | **接口类型示例重构**：现有 `if_raw_material` 示例与抽象父类概念重叠，需替换为跨继承树的示例（推荐"可溯源 Traceable"）。方案见 [接口类型示例重构分析.md](接口类型示例重构分析.md) | 中 |
| 5 | **链接类型删除审核机制**：当前删除链接类型直接生效，无审核确认流程 | 低 |
